---
paths:
  - 'app/Models/**'
---

# Models

## Mass assignment via #[Fillable] attribute
Declare mass-assignment allow-lists with the PHP 8 #[Fillable([...])] attribute, not the $fillable / $guarded properties.
