---
paths:
  - 'tests/**'
---

# Tests

## Pest with global RefreshDatabase
Feature tests use Pest syntax (test()/it()/expect()) and rely on RefreshDatabase applied globally via Pest.php. Create records via model factories.
